# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Realm Master' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Realm Master
  pod 'RealmSwift'

  target 'Realm MasterTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Realm MasterUITests' do
    # Pods for testing
  end

end
